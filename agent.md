# 星见塔罗｜Agent 接手说明

最后核对日期：2026-09-12
当前分支：`main`
当前基线：`6bf0131`，工作区包含待部署的 DeepSeek 解牌功能
GitHub：https://github.com/ZlyCatherine/star-tarot
GitHub Pages：https://zlycatherine.github.io/star-tarot/

## 1. 项目目标与一期范围

这是一个适合手机端使用的 Rider–Waite–Smith 塔罗抽牌应用。用户先选择牌阵，再自行洗牌、选牌、确认、翻牌并阅读简短牌义，也可通过受保护的 Cloudflare Worker 调用 DeepSeek 生成完整牌阵解读。前端通过 GitHub Pages 对外分享。

一期已确认范围：

- 使用完整的 78 张 Rider–Waite–Smith 牌组。
- 每张牌支持正位和逆位；画面朝向以用户视角为准。
- 用户先主动选择牌阵。
- 暂无账号、数据库、分享功能和缓存需求。
- 当前仅用 `sessionStorage` 保存当前标签页的 AI 访问码；产品侧允许未来加入本地牌阵记录。
- “快速回答”已从一期移除。
- 手机端体验优先，同时兼容桌面端鼠标操作。

当前牌阵：

1. 今日指引：1 张牌。
2. 事物发展：过去／现在／未来。
3. 建议牌阵：现状／阻碍／建议。
4. 恋人十字：上方“你的想法”、下方“对方想法”、左侧“过去”、中间“关系现状”、右侧“未来发展”。

首页牌阵卡片保持 `2 × 2` 排列，手机端也沿用两列。

## 2. 用户流程

应用主流程由 `Stage` 控制：

```text
select（选择牌阵）
  → focus（填写可选问题）
  → shuffle（洗牌）
  → draw（依次选牌、确认、翻牌、查看解读）
```

交互原则：

- 用户可以洗任意次数，界面不宣布“牌已经洗好”，也不催促用户停止或继续。
- 洗牌页只需要简洁说明和牌堆反馈。避免出现“手势洗牌”“正在记录轨迹”“松手后保存轨迹”“可以继续洗牌”等技术性或提示性文案。
- 洗牌页不显示圆形进度条、次数进度、状态胶囊或自动完成暗示。
- 抽牌区采用圆弧牌环，用户通过手指或鼠标拖动整副牌。不要改回平铺列表、横向滚动条或普通轮播。
- 点击与拖动必须清晰区分。轻点牌背进入确认弹窗；拖动期间不得误选。
- 点击一张牌后必须询问“确定选择这张牌吗？”。“再看看”关闭弹窗并返回同一牌环；“确定选择”让该张牌离开牌环并进入当前牌位。
- 用户选择的是牌环中的特定牌。相关文案使用“这张已选好的牌”“你选定的牌”等明确措辞，避免“任意一张牌”。
- 所有牌抽完以后，用户再依次翻牌。最终解读区标题只保留“牌面解读”，不增加引导、规训或暗示用户应该如何理解结果的标题。
- 全部牌面揭示后显示“AI 综合解读”区域；访问码在当前标签页有效，DeepSeek API Key 始终保留在服务端 Secret 中。

## 3. 随机与洗牌实现

核心代码位于 `src/App.tsx`。

### 逻辑牌序

- `prepareDeck()` 对 78 张牌执行 Fisher–Yates 洗牌。
- 每张牌的正逆位通过同一个随机源以 50% 概率决定。
- 用户选择“帮我洗牌”时，随机源为 `window.crypto.getRandomValues()`。
- 用户自行洗牌时，每次有效手势记录坐标、时间、压力和位移。
- 有效手势至少包含 3 个采样点，并且累计移动距离至少为 36px。
- 最多保存最近 10 次有效手势，超过后覆盖最早的数据，常量为 `MAX_GESTURE_PASSES = 10`。
- 每次进入洗牌页都会生成新的系统随机盐。最近 10 次手势与随机盐序列化后经过 SHA-256，摘要作为伪随机生成器的种子，再用于 Fisher–Yates 和正逆位判定。
- 单次手势最多保存 720 个采样点，避免无限增长。

### 洗牌动画

- 视觉牌堆固定使用 22 张牌背，常量为 `SHUFFLE_CARD_COUNT = 22`；它只是动画层，逻辑牌组始终是 78 张。
- 每张视觉牌拥有持续存在的 `ShuffleCardPose`，包含位置、旋转、速度、角速度和层级。
- 手势移动对现有牌施加扰动，随后由 `requestAnimationFrame` 更新惯性、阻尼和边界。
- 手势期间不要动态新增、销毁或更换视觉牌节点，否则用户会感觉牌突然生成或消失。
- 保持所有牌互相半覆盖的牌堆关系，不设置固定悬浮在最上方的“顶牌”。
- 松手后牌的位置可以保留乱序感，无需用明显的弹簧动画强制收拢成整齐牌堆。

## 4. 圆弧牌环实现

- 每张逻辑牌在首次洗牌后获得稳定的 `wheelSlot`。
- 牌环步进为 `360 / 78`，CSS 变量 `--wheel-angle` 决定每张牌在圆盘上的角度。
- 整个 `.card-wheel-rotor` 围绕屏幕下方的圆心旋转，牌背形成可拖动圆弧。
- 拖动使用 Pointer Events，同时支持触摸和鼠标；触摸增益为 `1.65`，鼠标增益为 `1.4`。
- 松手后根据角速度继续惯性滚动，并逐帧衰减。
- `syncWheelLayers()` 根据旋转后的角度更新可见牌的 `z-index`，避免顶部牌长期固定或两侧牌层级卡顿。
- 拖动超过 6px 后进入拖动状态，并短暂抑制点击。这个阈值和 `suppressCardClickUntilRef` 是解决“能滑动但点不了牌”及误触问题的关键。
- 牌按钮的悬停效果不要改变实际命中区域或触发布局重排，避免边缘剧烈抖动。
- 牌确认后延迟 460ms 从 `deck` 移除，以配合离场动画；原 `wheelSlot` 保留，剩余牌不会集体跳位。

## 5. AI 解牌实现

- 前端通过 `VITE_TAROT_AI_API_URL` 指向 Cloudflare Worker，并调用 `POST /interpret`。
- 用户输入的是应用访问码 `TAROT_ACCESS_CODE`；访问码保存在 `sessionStorage`，关闭标签页后清除。
- DeepSeek API Key 只配置为 Cloudflare Secret `DEEPSEEK_API_KEY`，不得写入前端环境变量、代码、GitHub 仓库或 GitHub Pages 构建产物。
- Worker 默认调用 `https://api.deepseek.com/v1/chat/completions`，模型为 `deepseek-v4-flash`，关闭思考模式，开启 JSON Output，使用非流式响应。
- 请求包含用户问题、牌阵、牌位、牌名、正逆位、关键词与基础牌义；服务端再次校验牌阵和牌位顺序。
- 返回结构固定为 `overview`、`positions`、`connections`、`answer`、`reflection`；Worker 校验结构后才交给前端显示。
- Worker 仅允许 GitHub Pages 正式来源和本地 `5173`、`4173` 端口；浏览器请求还需通过访问码验证。
- 不保存问题、牌面、解读结果或调用日志到数据库。
- 系统提示要求使用可能性语言；“对方想法”仅解释牌阵呈现的关系视角，不断言他人的真实心理。

## 6. 视觉与文案约束

- 主视觉为深紫黑、旧金色、象牙白，整体偏安静、神秘、克制。
- 图标统一使用 `lucide-react`，保持一致的尺寸、描边和按钮对齐方式。
- 避免加入解释算法、记录过程或催促节奏的 UI。
- 保持界面文案自然简洁，不向用户解释随机盐、轨迹采样或动画物理细节。
- 洗牌画面内不显示装饰性圆盘进度环和无意义的放射线。
- 牌环底部留白控制在约页面可视高度的一半以内，手机端避免出现大块空区。
- 弹窗按钮、牌阵卡片、解读卡片需要检查窄屏换行和安全区。

## 7. 代码结构

- `src/App.tsx`：页面流程、状态、手势采样、洗牌随机、洗牌动画、圆弧牌环、抽牌确认和翻牌逻辑。
- `src/styles.css`：完整视觉样式和响应式规则，关键断点为 `900px`、`620px`，另有矮屏横屏和减少动画偏好处理。
- `src/tarot-data.ts`：78 张牌的中英文名称、关键词、正逆位解读、图片文件名及四种牌阵定义。
- `src/ai-types.ts`：前端与 Worker 共识的 AI 请求、解读与用量类型。
- `src/ai-client.ts`：前端 AI 请求、错误映射和响应结构校验。
- `worker/src/index.js`：访问码校验、输入校验、DeepSeek 调用、JSON 输出校验和 CORS。
- `worker/src/index.test.js`：Worker 数据、鉴权和 DeepSeek 请求契约测试。
- `worker/wrangler.toml`：Cloudflare Worker 配置，默认模型为 `deepseek-v4-flash`。
- `.env.example` 与 `worker/.dev.vars.example`：仅含变量名和占位符，不得填入真实密钥后提交。
- `public/cards/`：78 张历史 RWS 牌面扫描图。
- `components/ui/`：通用 UI 组件；当前核心页面主要使用 Button 和 Dialog。
- `CREDITS.md`：牌面和牌义来源说明。
- `.github/workflows/deploy.yml`：`main` 分支推送后的 GitHub Pages 构建和部署。
- `vite.config.ts`：使用相对路径 `base: './'`，确保 GitHub Pages 子路径部署可用。

## 8. 本地开发与验证

环境要求：Node.js `>=22.13.0`。

```bash
npm install
npm run dev
npm run build
npm run test:worker
```

`npm run build` 同时执行 TypeScript 检查和 Vite 生产构建。提交前至少运行：

```bash
npm run build
npm run test:worker
git diff --check
```

建议人工回归：

- 首页四种牌阵在手机宽度下保持 2 × 2，文字无严重溢出。
- 鼠标画圈和手指画圈都能扰动牌堆，多次洗牌不会出现新增牌节点的错觉。
- 未完成一次有效手势时，“完成洗牌”不可用；“帮我洗牌”可直接进入抽牌。
- 圆弧牌环可以顺畅拖动，有适度惯性，顶部牌会随圆盘连续变化。
- 轻点牌背能稳定弹出确认框；小幅移动不会造成抖动或错误选择。
- 取消确认后仍能继续拖动和选择；确认后只有该牌离开牌环。
- 三张牌和五张牌阵的牌位、抽牌顺序与布局一致。
- 所有牌抽完后才允许翻牌，正逆位图像方向和解读一致。
- 320–430px 宽的竖屏、常见桌面宽度、矮屏横屏均需检查。
- 开启 `prefers-reduced-motion` 时仍可完成全部操作。
- 完成翻牌后，未输入访问码时会弹出访问码对话框；取消后原牌阵保持不变。
- 有效访问码能生成五个固定区块；取消请求、网络失败、访问码错误和 DeepSeek 异常都有可恢复提示。
- 手机端 AI 区域、访问码对话框、长文本和重新解读按钮无横向溢出。

在受限沙箱中，Vite 可能因原生依赖加载或 `spawn EPERM` 构建失败。此前在获得相应执行权限后构建成功；遇到同类错误时先确认沙箱权限，无需立刻重装依赖。

## 9. 部署与仓库状态

- 远端名称：`origin`
- 默认分支：`main`
- 推送到 `main` 后由 GitHub Actions 自动部署 Pages。
- Worker 需要单独部署到 Cloudflare；线上配置 `DEEPSEEK_API_KEY` 与 `TAROT_ACCESS_CODE` 两个 Secret。
- GitHub Actions Variables 需要配置公开变量 `VITE_TAROT_AI_API_URL`，值为 Worker 根地址。
- 发布前检查 `git status`，只提交本次任务涉及的文件，保留用户已有改动。
- 图片授权和牌义来源变更时同步更新 `CREDITS.md`。

## 10. 后续可能事项

- 本地保存历史牌阵或未完成抽牌：产品侧已允许存储，当前尚未实现。
- 分享结果：一期明确暂缓。
- 若继续优化动画，优先保证节点连续性、触摸响应、点击命中和低端手机帧率。
- 若调整文案，维持用户主动选择与自主节奏，避免状态催促和行为规训。
- 当前面向一两位轻度用户，未实现复杂限流；若访问范围扩大，可增加 Cloudflare Rate Limiting、访问码轮换或每码额度。
